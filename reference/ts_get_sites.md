# Get Site Table

Gets site table as a data frame.

## Usage

``` r
ts_get_sites(conn = getOption("tsdbr.conn", NULL))
```

## Arguments

- conn:

  An object of class SQLiteConnection.

## Value

A data frame of the requested data.
